# Port for the Express server
PORT=5000

# Simple operator PIN (replaces full auth for MVP - see middleware/auth.js)
OPERATOR_PIN=1234

# Comma-separated list of allowed frontend origins (for CORS)
FRONTEND_ORIGIN=http://localhost:5173

NODE_ENV=development

# ─── Database ─────────────────────────────────────────────────────────
# If you have MongoDB installed locally (default port 27017):
MONGO_URI=mongodb://localhost:27017/cablesync

# If you prefer MongoDB Atlas (cloud), use your connection string:
# MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/cablesync
MONGO_URI=mongodb+srv://apsfl_admin:Apsfl2026@cluster0.vpqyh1r.mongodb.net/cablesync?retryWrites=true&w=majority&appName=Cluster0

# Frontend URL for CORS
CLIENT_URL=http://localhost:5173